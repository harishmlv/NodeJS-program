// Convert Array String name from Lower case to Upper case.

var list = ["herry malviya"];
var max=0,k;
for(i=0;i<list.length;i++)
{
    console.log(list[i].toUpperCase());
}

// Convert Array String name from Upper case to Lower case.

var list = ["HERRY MALVIYA"];
var max=0,k;
for(i=0;i<list.length;i++)
{
    console.log(list[i].toLowerCase());
}